---
interface Props {
  href: string;
  title: string;
  className?: string;
}

const { title, href, className } = Astro.props;
---

<a href={href} class=`text-sm md:text-lg  transition-colors ${className}`
  >{title}</a
>
