---
interface Props {
  href: string;
  title: string;
  className?: string;
  download?: boolean
}

const { title, href, download,className } = Astro.props;
---

<a href={href} download={download} class=`text-sm md:text-lg  transition-colors ${className}`
  >{title}</a
>
