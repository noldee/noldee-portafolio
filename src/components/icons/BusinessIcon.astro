---
interface Props {
  href: string;
  className?: string;
}

const { href, className = "" } = Astro.props;
---

<a
  href={href}
  class={`bg-[#1c2026] p-2 rounded ${className}`}
  target="_blank"
  rel="noopener noreferrer"
>
  <slot />
</a>
