---
interface Props {
  href: string;
  title: string;
  class: string;
}

const { title, href, class: className } = Astro.props;
---

<a
  href={href}
  class=`text-sm md:text-lg hover:text-white transition-colors ${className}`
  >{title}</a
>
